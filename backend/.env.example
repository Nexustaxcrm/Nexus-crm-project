# ============================================
# Nexus CRM - Environment Variables Template
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
# PostgreSQL database connection settings
DB_USER=postgres
DB_HOST=localhost
DB_NAME=nexus_crm
DB_PASSWORD=your_password_here
DB_PORT=5432

# Database SSL (set to 'true' for Railway/cloud databases)
# Railway databases require SSL
DB_SSL=false

# ============================================
# AUTHENTICATION (REQUIRED)
# ============================================
# JWT Secret for signing authentication tokens
# Generate a secure secret with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here

# ============================================
# SERVER CONFIGURATION (OPTIONAL)
# ============================================
# Server port (defaults to 3000)
# Railway automatically sets PORT, so you may not need this
PORT=3000

# Default admin password (optional, defaults to 'admin123')
# This is the password for the default admin user created on first startup
# IMPORTANT: Change this password after first login!
ADMIN_PASSWORD=admin123

# ============================================
# EMAIL CONFIGURATION (OPTIONAL)
# ============================================
# Email features require Gmail App Password
# See backend/EMAIL_SETUP.md for instructions on creating App Password

# Email user (defaults to nexustaxfiling@gmail.com)
EMAIL_USER=nexustaxfiling@gmail.com
# Alternative variable name:
# GMAIL_USER=nexustaxfiling@gmail.com

# Gmail App Password (16-character password from Google Account settings)
# This is NOT your regular Gmail password!
EMAIL_PASSWORD=your_gmail_app_password_here
# Alternative variable name:
# GMAIL_APP_PASSWORD=your_gmail_app_password_here

# ============================================
# AWS S3 CONFIGURATION (OPTIONAL)
# ============================================
# S3 storage for persistent file storage
# Without S3, files are stored locally and may be lost during redeployments
# See backend/S3_SETUP_GUIDE.md for detailed setup instructions

# AWS Access Key ID
AWS_ACCESS_KEY_ID=your_access_key_id_here

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# AWS Region (defaults to us-east-1)
AWS_REGION=us-east-1

# S3 Bucket Name
AWS_S3_BUCKET_NAME=your_bucket_name_here

# S3 Prefix/Folder (optional, defaults to 'customer-documents')
# Files will be stored in: s3://bucket-name/customer-documents/
AWS_S3_PREFIX=customer-documents

# ============================================
# NOTES
# ============================================
# 1. Required variables:
#    - DB_USER, DB_HOST, DB_NAME, DB_PASSWORD, DB_PORT
#    - JWT_SECRET
#
# 2. Optional variables:
#    - EMAIL_USER, EMAIL_PASSWORD (for email features)
#    - AWS_* variables (for S3 file storage)
#    - ADMIN_PASSWORD (defaults to 'admin123')
#    - PORT (defaults to 3000)
#
# 3. For Railway deployment:
#    - Set all variables in Railway dashboard â†’ Variables tab
#    - Database variables are auto-set when you add PostgreSQL service
#    - Generate a new JWT_SECRET for production
#
# 4. Security:
#    - Never commit .env file to Git
#    - Use strong, unique JWT_SECRET
#    - Change default admin password after first login
#    - Use Gmail App Password, not regular password
#
# 5. See backend/SETUP_ENV.md for detailed setup instructions
